version: '3'
services:
  backend:
    container_name: backend
    env_file: .env
    build:
      context: backend
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - .:/back
    ports:
      - 3000:3000